p1:
  id: syvai/p1-segments
  subset: null
  train_name: train
  text_column: text
  audio_column: audio
  filter_dataset: true
  language: da
  # The revision is supplied by the production gate, never a moving branch.
  revision: ${oc.env:P1_SEGMENTS_REVISION,""}
  immutable_revision_env: P1_SEGMENTS_REVISION
  trust_remote_code: false
